version: '3.4'

services:
  app: 
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - JAVA_OPTS
        - SDK_OPTS
    environment:
      - JAVA_OPTS
      - http_proxy
      - https_proxy
      - PORT
      - ANDROID_ADB_SERVER_ADDRESS
      - YARN_RESIGSTRY
    command: yarn example start
    ports: 
      - "$PORT:8081"
    volumes:
      - .:/srv
